# Build output (generated by build.js / deploy.ps1 from the source pages)
_deploy/
_test_i18n/
espeledata-site.zip

# Generated CV PDFs live in CV/; cv-src/ holds only the HTML sources + renderer
cv-src/*.pdf

# Local tooling state (machine-specific, not part of the site)
.claude/
.netlify/

# Dependencies
node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
